My recommendation: use the **full version** at project startup, and the **short version** for follow-up Codex sessions in the same repo.

prompt>

Use the repository control system.

- Read `README.md`, `AGENT_RULES.md`, relevant `docs/starter/*`, and relevant `docs/project/*`
- Treat `docs/project/*` as the source of truth
- Do not infer missing architecture or stack details
- For non-trivial work: provide guidance context, execution readiness, and verification plan before implementation
- Keep changes small and scoped
- Update `docs/project/*` when new repo-specific truth is discovered
- Explicitly state what was verified, how it was verified, and which checks were applied
- Use or update a work loop for non-trivial tasks